import React from 'react';

const Header = () => {
    return (
        <div className="hero">
            <div className="hero-body">
                <h1>Värm ditt inre med årets glögg</h1>

                <button className="btn btn-primary btn-primary__hero">
                    Se årets smaker
                </button>
            </div>
        </div>
    );
};

export default Header;
